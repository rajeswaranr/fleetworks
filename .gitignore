# Local Claude Code tooling (preview server config)
.claude/

# Supabase CLI local state (which project is linked, auth cache)
supabase/.temp/

# Local generated seed data / browser storage state
.tmp/
